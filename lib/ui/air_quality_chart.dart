import 'package:flutter/material.dart';

class AirQualityChart extends StatelessWidget {
  const AirQualityChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
